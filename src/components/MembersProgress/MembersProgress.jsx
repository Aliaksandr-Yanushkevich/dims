import React from 'react';
import { NavLink } from 'react-router-dom';
import style from './MembersProgress.module.scss';

const MembersProgress = (props) => {
  return <div>MembersProgress</div>;
};

export default MembersProgress;
